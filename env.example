# This file serves as an example for the .env file.
# Copy this file to .env and fill in your actual credentials and configurations.
# Do NOT commit your .env file to version control.

# --- Speech-to-Text ---
# Deepgram API Key for transcription
DEEPGRAM_API_KEY=your_deepgram_api_key

# --- Text-to-Speech ---
# ElevenLabs API Key for speech synthesis
ELEVENLABS_API_KEY=your_elevenlabs_api_key

# Sesame CSM Text-to-Speech service URL (if using Sesame CSM provider)
# Example: SESAME_CSM_URL=http://localhost:5001
SESAME_CSM_URL=

# --- Dialogue Management ---
# Base URL for your Rasa server
# Example: RASA_URL=http://localhost:5005
RASA_URL=http://localhost:5005

# --- Telephony (Twilio) ---
# Twilio Account SID for voice capabilities
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Twilio Auth Token
TWILIO_AUTH_TOKEN=your_twilio_auth_token

# --- Real Estate Knowledge Service (Optional) ---
# Credentials for connecting to a PostgreSQL MLS database if the
# RealEstateKnowledgeService is used and configured for PostgreSQL.
MLS_DB_USER=
MLS_DB_PASSWORD=
MLS_DB_HOST=localhost
MLS_DB_PORT=5432
MLS_DB_NAME=mls_data

# --- Natural Language Generation (Optional - if using cloud providers) ---
# API key if using OpenAI GPT models via the natural_language_generation_service
# OPENAI_API_KEY=your_openai_api_key

# API key if using Anthropic Claude models via the natural_language_generation_service
# ANTHROPIC_API_KEY=your_anthropic_api_key

# --- MCPS Platform Integration (Optional) ---
# URL for the MCPS server if mcps_integration is enabled in config.yml
# MCPS_SERVER_URL=http://localhost:8080
